<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssg.wsmt.smOrders.mapper.ShoppingMallMapper">

    <select id="selectAll" resultType="com.ssg.wsmt.smOrders.domain.ShoppingMallVo">
        SELECT * FROM shopping_mall
    </select>

    <select id="findAll" resultType="com.ssg.wsmt.smOrders.domain.ShoppingMallVo">
        SELECT * FROM shopping_mall
    </select>

</mapper>